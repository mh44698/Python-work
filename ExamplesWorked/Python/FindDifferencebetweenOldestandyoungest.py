def difference_in_ages(ages):
    maximimum = max(ages)
    minimum = min(ages)
    difference = maximimum - minimum
    output = (minimum,maximimum,difference)
    # your code here
    return output
